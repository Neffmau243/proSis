# Base de la API. En desarrollo el proxy de Vite reenvía /api al backend
# (ver vite.config.ts), por lo que basta la ruta relativa.
# Para apuntar a otro host use una URL absoluta, p.ej. http://127.0.0.1:8000/api/v1
VITE_API_BASE_URL=/api/v1